import styled from "@emotion/styled";

export const StyledPassengerStep = styled.fieldset`
  border: none;
  .passenger-step {
    &__hint {
      margin-bottom: 20px;
    }

    &__add-btn {
      display: block;
      width: 100%;
    }
  }
`